const color = {
  black: '#000000',
  grey: '#D9D9D9',
  blue: '#0094FF',
  white: '#FFFFFF',
};

const bgColor = {
  primary: color.black,
  secondary: color.grey,
  tertiary: color.blue,
};

const fontColor = {
  primary: color.white,
  secondary: color.black,
  tertiary: color.white,
};

const theme = {
  color,
  bgColor,
  fontColor,
};

export default theme;
